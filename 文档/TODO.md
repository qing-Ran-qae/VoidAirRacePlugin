# TODO

## 统一注册表（1.）、完善用户包机制（2.）

这两个步骤部分有交叉

---

找一个安全、<del>持续更新</del>的lua库

---

考虑将注册表类内部维护双层表改成单层表记录“注册项类别”（新类型）对象

原`EntryCategory`改成`CategoryId`，通过id对象从注册表单例获取由注册表维护的类别实例

所有注册项类别继承同一基类，基类提供基本地查询和项目记录功能，子类可实现更高级功能（如双射）

## 后续

3. 比赛异常结束后恢复
4. 事件优先级控制初始化改成CI
5. 结果类型区分”技术性消息”（详细，可包含服务端参数）和“用户消息”（不详细，禁止包含服务端参数）
6. 增加编译期扫描类的工具提升启动速度
